import React from 'react'

const CallDaysTabContent = ({ title, content }) => {
  return (
    <div>
      <h1>{title}</h1>
      <div>{content}</div> 
    </div>
  )
}

export default CallDaysTabContent